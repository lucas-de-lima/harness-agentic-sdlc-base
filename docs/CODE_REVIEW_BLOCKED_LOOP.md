# Blocked Loop

A review becomes `Blocked` when the reviewer cannot make a safe decision because:

- required artifacts are missing
- repository state is ambiguous
- required architecture evidence is contradictory
- required external validation is unavailable

The task remains blocked until the prerequisite is resolved.
