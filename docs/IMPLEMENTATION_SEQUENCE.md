# Implementation Sequence

A default sequence is:

Foundation → core behavior → persistence/integrations → interface → validation → hardening.

Respect the approved architecture and parallelize only when dependencies and file ownership make it safe.
